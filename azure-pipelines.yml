# azure-pipelines.yml

pool:
  #name: Default
  vmImage: 'ubuntu-latest'

steps:
  - task: k6-load-test@0
    inputs:
      cloud: true      
      args: -o experimental-prometheus-rw
      filename: './scripts/test_times.js'